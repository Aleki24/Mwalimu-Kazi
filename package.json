{
  "name": "mwalimu-kazi",
  "private": true,
  "engines": {
    "node": ">=20.11"
  },
  "scripts": {
    "build": "turbo run build",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "turbo run lint",
    "db:types": "supabase gen types typescript --local > packages/types/src/database.generated.ts"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "turbo": "^2.3.3",
    "typescript": "^5.9.3",
    "vitest": "^2.1.9",
    "zod": "^4.5.4"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
